[project]
name = "zagtag-extension-sdk"
version = "0.1.0"
description = "Python SDK for authoring Zagtag extensions (HTTP loopback protocol)."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/zagtag_extension_sdk"]
